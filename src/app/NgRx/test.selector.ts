// import { createFeatureSelector, createSelector } from "@ngrx/store";
// import { CounterState } from "./test.state";

// // Feature selector to select the counter state
// export const selectCounterState = createFeatureSelector<CounterState>('counterState');
  
// // Selector to select the counter value from the counter state
// export const selectCounterValue = createSelector(
//   selectCounterState,
//   (state: CounterState) => state.counter
// );